{
  "name": "overlink",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:web": "npm run dev -w @overlink/web",
    "build:web": "npm run build -w @overlink/web",
    "lint:web": "npm run lint -w @overlink/web",
    "build:extension": "npm run build -w @overlink/extension"
  }
}
