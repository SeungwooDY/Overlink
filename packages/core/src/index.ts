/**
 * @overlink/core — Shared Processing Engine
 *
 * Phase 1: Placeholder only.
 *
 * Future exports:
 *   - frameHash(canvas)      → perceptual hash for change detection
 *   - extractURLs(text)      → regex-based URL extraction
 *   - runOCR(canvas)         → tesseract.js wrapper
 *   - shared types (DetectedURL, FrameResult, …)
 *
 * Both apps/extension and future apps/desktop will import from here.
 */

export {};
